# The Pet Project - NextJS Application

## Development
Local development uses local docker image.

> Use the root `docker-compose` configuration to run the app locally.

## Production
Next application deployed to Netlify.
